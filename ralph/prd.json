{
  "project": "SeeFlow Live Share",
  "branchName": "ralph/live-share-phase-5",
  "description": "Phase 5 \u2014 presence + remote cursors + attribution toasts.",
  "userStories": [
    {
      "id": "US-045",
      "title": "Add cursor envelope frame type + Zod payload schemas",
      "description": "As a relay maintainer, I need the WebSocket envelope to formally support a 'cursor' frame type (alongside existing 'presence') with a typed payload, so host and peers can exchange high-frequency cursor positions without bypassing Zod validation.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer/cloud",
        "Edit cloud/lambda/share/shared/envelope.ts: add 'cursor' to the EnvelopeSchema type enum, alongside the existing 'presence' literal",
        "Add and export `PresencePayloadSchema` = z.object({ peerId: z.string(), displayName: z.string().max(40), color: z.string().regex(/^#[0-9a-fA-F]{6}$/), state: z.enum(['joined', 'left', 'active', 'idle']), flowId: z.string().optional() })",
        "Add and export `CursorPayloadSchema` = z.object({ peerId: z.string(), flowId: z.string(), x: z.number().finite(), y: z.number().finite(), viewport: z.object({ zoom: z.number(), x: z.number(), y: z.number() }).optional(), ts: z.number().int().nonnegative() })",
        "Export inferred types `PresencePayload` and `CursorPayload`",
        "Update cloud/lambda/share/shared/envelope.test.ts: add tests asserting (a) a valid `cursor` envelope parses, (b) presence with displayName >40 chars is rejected, (c) cursor with NaN x/y is rejected, (d) cursor with malformed color hex (for presence) is rejected",
        "`bun test lambda/share/shared/envelope.test.ts` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-046",
      "title": "Maintain presence ledger and broadcast join/leave on relay",
      "description": "As a peer, I want to receive a presence broadcast whenever any peer joins, leaves, or is kicked, so the local presence store always reflects who is currently connected.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer/cloud",
        "Edit cloud/lambda/share/ws-default.ts: after `handleAuthPeer` successfully calls `addPeer`, fan out a `{ v:1, type:'presence', from: connId, to:'all', payload: { peerId, displayName, color, state:'joined' } }` envelope to every other peer AND to the host",
        "Edit cloud/lambda/share/ws-disconnect.ts: after `removePeer` or `setHostConnId(null)`, broadcast `{ type:'presence', payload: { peerId, displayName, color, state:'left' } }` to remaining peers; when host disconnects, broadcast a `state:'host-offline'` presence variant so peers can render banners (extend `PresencePayloadSchema` if needed to include 'host-offline')",
        "Generate a deterministic peer color server-side: HSL hue = peerId hashed via crypto.createHash('sha256') mod 360, S=70%, L=55%, formatted as #RRGGBB; store color in PeerInfo so the same peer keeps the same color across frames",
        "Edit cloud/lambda/share/shared/sessions.ts: extend `PeerInfo` with `color: string` field; update `addPeer` typings and tests",
        "Add cloud/lambda/share/ws-default.presence.test.ts: assert (a) auth-peer success triggers exactly one presence broadcast to host + peers, sender excluded, (b) handler does not crash when fan-out target is `GoneException` and continues with remaining recipients",
        "Add a corresponding test in ws-disconnect.test.ts: assert leave broadcast is sent on peer disconnect and host-offline broadcast on host disconnect",
        "`bun test lambda/share/ws-default.presence.test.ts lambda/share/ws-disconnect.test.ts lambda/share/shared/sessions.test.ts` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-047",
      "title": "Relay cursor frames with size + rate guards",
      "description": "As a relay operator, I want cursor frames routed only to fellow peers + host with strict per-connection rate limits, so a malicious or buggy peer cannot saturate the WebSocket API.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer/cloud",
        "Edit cloud/lambda/share/ws-default.ts `routeFrame`: when envelope.type === 'cursor', accept only when sender is authenticated, force `to = 'all'` regardless of incoming value (cursors are public to the session), and re-validate payload via `CursorPayloadSchema.safeParse` \u2014 drop with no broadcast on failure",
        "Implement per-connection in-memory rate limiter using a fixed lambda-scope `Map<connId, { tokens: number; ts: number }>`: 45 tokens/sec refill, 60 token bucket \u2014 drop (no error reply) when bucket empty; covers only `cursor` and `presence` types so RPC paths are unaffected",
        "Lambdas continue to log only `{ type, from, to, sizeBytes, dropped?: 'rate' | 'invalid' }` \u2014 never payload",
        "Reject `cursor` frames where serialized JSON size > 512 bytes with `dropped:'oversize'` (defensive cap; valid frames are ~120 B)",
        "Add cloud/lambda/share/ws-default.cursor.test.ts: assert (a) authed peer cursor fans out to host + other peers but not sender, (b) unauthed connId is rejected with no broadcast, (c) 200 frames in 1s from a single peer result in roughly 60 broadcasts (bucket size) and the rest dropped, (d) oversize payload is dropped, (e) invalid payload (`x: 'foo'`) is dropped",
        "`bun test lambda/share/ws-default.cursor.test.ts` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-048",
      "title": "Add presenceLayer + onCursorMove props to SeeflowCanvas",
      "description": "As a canvas consumer, I want a `presenceLayer` ReactNode slot and an `onCursorMove(flowCoords, viewport)` callback on `<SeeflowCanvas>`, so peer SPAs and host studio can render remote cursors and emit local cursor updates without forking the canvas.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow",
        "Edit packages/canvas/src/components/seeflow-canvas.tsx `SeeflowCanvasProps`: add optional `presenceLayer?: ReactNode` and optional `onCursorMove?: (coords: { x: number; y: number; flowId: string | null }, viewport: { x: number; y: number; zoom: number }) => void`",
        "Render `presenceLayer` inside the React Flow viewport stacking context (after `<Background>`, before `<Controls>`) so it transforms with pan/zoom and inherits pointer-events:none unless individual cursors override",
        "Inside the canvas pane's existing `onPointerMove` handler, when `onCursorMove` is provided, call `useReactFlow().screenToFlowPosition()` to convert clientX/Y to flow coords and invoke the callback; pass `viewport` from `useViewport()`",
        "When `onCursorMove` is undefined the canvas behaves exactly as before \u2014 no extra work in the pointer handler",
        "Export `presenceLayer` and `onCursorMove` types from packages/canvas/src/index.ts (already re-exports SeeflowCanvasProps)",
        "Add packages/canvas/src/components/seeflow-canvas.presence.test.tsx: render canvas with a stub `presenceLayer={<div data-testid=\"layer\" />}` and assert the node is in the DOM inside the viewport wrapper",
        "Add a test that fires a `pointermove` on the pane and asserts `onCursorMove` is called with finite x/y and zoom matching the current viewport",
        "`bun test packages/canvas/src/components/seeflow-canvas.presence.test.tsx` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-049",
      "title": "Build PeerCursorsLayer overlay component in canvas package",
      "description": "As a peer or host viewer, I want remote cursors rendered as a small arrow + display-name label at each peer's flow-space coordinates, so I can see what others are pointing at.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow",
        "Create packages/canvas/src/components/peer-cursors-layer.tsx exporting `PeerCursorsLayer` (props `{ peers: Array<{ peerId: string; displayName: string; color: string; x: number; y: number; idle?: boolean }>; selfPeerId?: string }`)",
        "Filter out `selfPeerId` from rendering; one positioned SVG arrow + name pill per remaining peer, absolutely positioned at `translate({x}px, {y}px)` inside the viewport-transformed wrapper so React Flow pan/zoom moves them",
        "Use the design tokens from design/design.html: name pill = peer color background, white text, JetBrains Mono 11px, 4px radius, 4px/6px padding; arrow = 14\u00d714 SVG in peer color with subtle drop-shadow",
        "Apply `opacity: 0.4` when `idle` is true; use CSS transition `transform 80ms linear` for smoothing between frames (low-pass that hides jitter without lag)",
        "Export `PeerCursorsLayer` and `PeerCursorsLayerProps` from packages/canvas/src/index.ts under the Chrome section",
        "Add packages/canvas/src/components/peer-cursors-layer.test.tsx: assert (a) selfPeerId is excluded, (b) two peers render two arrows at correct transforms, (c) idle peer renders with opacity 0.4, (d) name labels render the displayName text",
        "`bun test packages/canvas/src/components/peer-cursors-layer.test.tsx` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 5,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-050",
      "title": "Wire cursor send/receive + throttling into peer share-client",
      "description": "As a peer browser, I need the share-client to emit my cursor at ~30 fps via a 'cursor' envelope and dispatch incoming cursors into a presence store, so remote peers can render my position and I can render theirs.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Edit src/lib/share-client.ts (or create if absent from phase 3): add `sendCursor({ flowId, x, y, viewport })` that wraps the call in a 33ms-window throttle via `requestAnimationFrame` + last-write-wins coalescing, drops if not authed, and sends `{ v:1, type:'cursor', from: <peerConnId>, to:'all', payload: { peerId, flowId, x, y, viewport, ts: Date.now() } }`",
        "Add a peer presence store `src/lib/presence-store.ts` exporting `createPresenceStore()` returning `{ subscribe, getSnapshot, applyPresence, applyCursor, markIdle }`; entries shape `{ peerId, displayName, color, cursor?: { x, y, flowId, lastTs }, state: 'active' | 'idle' | 'left' }`; `markIdle` flips state to 'idle' for any peer whose `lastTs` is older than 8s",
        "Wire incoming envelopes in share-client's onmessage: dispatch `presence` to `applyPresence`, `cursor` to `applyCursor`; ignore frames where `from === selfConnId`",
        "Idle sweep: `setInterval(markIdle, 4000)` cleared on store disposal",
        "Update src/pages/share-session.tsx (created in phase 3) to: subscribe to the presence store with `useSyncExternalStore`, pass `<PeerCursorsLayer peers={\u2026}>` as `<SeeflowCanvas presenceLayer={\u2026}>`, wire `onCursorMove` to call `shareClient.sendCursor(...)` with the current flowId",
        "Add src/lib/share-client.cursor.test.ts: assert (a) 100 sendCursor calls within 200ms result in <=12 actual frames emitted (throttling), (b) only the latest x/y from a burst is transmitted, (c) cursor frames are not emitted before auth completes",
        "Add src/lib/presence-store.test.ts: cover join/leave, idle sweep, self-exclusion",
        "`bun test src/lib/share-client.cursor.test.ts src/lib/presence-store.test.ts` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-051",
      "title": "Add presence sidebar and live peer badge to peer SPA",
      "description": "As a peer, I want a visible list of who else is connected (with avatars + names + colors) and a connection-status badge in the header, so I always know who's in the session.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Create src/components/presence-sidebar.tsx exporting `PresenceSidebar` (props `{ peers: Array<{ peerId; displayName; color; state }>; selfDisplayName: string; hostOnline: boolean }`)",
        "Layout: vertical stack docked right of canvas, 240px wide, dark theme tokens from src/lib/design-tokens.css; each row = circle badge (peer color, initial letter) + displayName + tiny status dot (green=active, amber=idle, gray=left); host row pinned to top with `(host)` suffix",
        "Create src/components/connection-status-badge.tsx: pill rendered in the existing `viewer-header.tsx`, showing `Connected \u00b7 N peers` when WS open + hostOnline, `Host offline` when host dropped, `Reconnecting\u2026` while ws.readyState !== OPEN",
        "Mount both in src/pages/share-session.tsx; PresenceSidebar gets data from the presence store via `useSyncExternalStore`",
        "Add src/components/presence-sidebar.test.tsx: assert host pin order, idle dot color, exclusion of `state:'left'` rows (or rendered grayed out)",
        "Add src/components/connection-status-badge.test.tsx: assert each of the three states renders the expected copy",
        "`bun test src/components/presence-sidebar.test.tsx src/components/connection-status-badge.test.tsx` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-052",
      "title": "Surface attribution metadata on host edit broadcasts",
      "description": "As a peer or host, I want every accepted remote edit to include the originating peer's id and displayName in the broadcast, so the UI can render 'Alice moved Node X' attribution without needing a separate presence lookup.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow/apps/studio",
        "Edit src/share.ts: when an `rpc` from a peer is accepted and `operations.ts` returns success, build the outgoing `node-patched` envelope payload with extra fields `{ flowId, nodeId, diff, version, attributedTo: { peerId, displayName } }`. Look up displayName from the in-memory share state's peer map keyed by `from` connId",
        "Mirror the same shape in the `rpc-result { ok:true }` reply so the originating peer can confirm its own attribution",
        "Edit src/share.ts host-local origination too: when the studio host applies an edit via the local UI, broadcast `node-patched` with `attributedTo: { peerId: 'host', displayName: <hostDisplayName from share state> }`",
        "Update Zod payload schema for `node-patched` in share.ts (or in a new shared/payloads.ts colocated module) to make `attributedTo` required, with peerId either equal to 'host' or matching a known peer",
        "Update apps/studio/src/share.test.ts (create if missing): cover (a) peer-originated rpc -> broadcast contains attribution, (b) host-originated edit -> broadcast attributes 'host', (c) unknown sender connId -> rpc rejected and no broadcast",
        "Append accepted-op audit record (`~/.seeflow/share-history/<sessionId>.jsonl`) already exists from earlier phases \u2014 extend the entry to include `attributedTo` field for completeness",
        "`bun test apps/studio/src/share.test.ts` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-053",
      "title": "Render attribution toasts on node-patched frames",
      "description": "As a peer or host viewer, I want a small toast like 'Alice moved Node X' to flash for ~2.5s on every accepted remote edit, so I always know who is changing what without having to watch the audit log.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow",
        "Create packages/canvas/src/components/attribution-toast.tsx exporting `AttributionToastStack` (props `{ items: Array<{ id: string; color: string; displayName: string; verb: string; nodeLabel: string; createdAt: number }>; onExpire(id) }`) \u2014 fixed-position stack bottom-left, max 3 visible, each toast 2500ms then `onExpire` fires; design/design.html tokens: dark surface, peer-color left border 3px, JetBrains Mono 12px",
        "Export `AttributionToastStack` and `AttributionToastItem` types from packages/canvas/src/index.ts",
        "Create packages/canvas/src/lib/format-attribution.ts exporting `formatAttribution(op: string, diff: object, nodeLabel?: string): { verb: string; nodeLabel: string }` mapping known ops to readable verbs: `moveNode\u2192'moved'`, `patchNode\u2192'updated'`, `addNode\u2192'added'`, `deleteNode\u2192'deleted'`, `addConnector\u2192'connected'`, `deleteConnector\u2192'disconnected'`, `addBulk\u2192'created'`, `reorderNode\u2192'reordered'`, fallback to op name",
        "Peer SPA: create seeflow-viewer/src/hooks/use-attribution-toasts.ts that subscribes to share-client `node-patched` frames, derives toast items via `formatAttribution`, dedupes within 200ms of same {peerId,nodeId,op}, and renders `<AttributionToastStack>` in src/pages/share-session.tsx",
        "Host studio: do the same in apps/studio/src/share.ts -> apps/web; create apps/web/src/hooks/use-attribution-toasts.ts and mount the stack from apps/web/src/app.tsx (or canvas.tsx) gated on `shareState.active`",
        "Toasts must NOT fire for `attributedTo.peerId === selfPeerId` \u2014 your own edits are silent",
        "Add packages/canvas/src/components/attribution-toast.test.tsx covering (a) stack caps at 3, (b) expires after 2500ms, (c) onExpire called with correct id",
        "Add packages/canvas/src/lib/format-attribution.test.ts covering all known ops + fallback",
        "Add seeflow-viewer/src/hooks/use-attribution-toasts.test.tsx covering self-suppression + dedupe window",
        "`bun test packages/canvas/src/components/attribution-toast.test.tsx packages/canvas/src/lib/format-attribution.test.ts` passes; viewer hook test passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 9,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-054",
      "title": "Propagate displayName end-to-end and persist self color",
      "description": "As a peer, I want my displayName from the join modal to flow through every presence/cursor/attribution surface and survive reconnect, so my identity is consistent across the session.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Edit src/pages/share-session.tsx: persist `{ displayName, peerId, color }` in sessionStorage under key `seeflow.share.identity.<token>` after the first join; on reload reuse without prompting unless missing or expired (>24h)",
        "Pass displayName into the `POST /api/share/join` request body (already wired in phase 1) and into the auth-peer frame payload so the relay's `addPeer` records the same name",
        "Ensure `share-client.sendCursor` includes the cached `peerId` and `color` so PeerCursorsLayer can render even before the first presence frame arrives from the relay",
        "Edit src/components/presence-sidebar.tsx (from US-051): always pin self row at top with `(you)` suffix when `selfPeerId` matches; never show self in PeerCursorsLayer",
        "Edit apps/studio/src/share.ts: the host's own attribution uses a `hostDisplayName` derived from `os.userInfo().username` (or `'Host'` fallback) and is included in the share state SSE so the local studio UI can render its own (suppressed) self-attribution consistently",
        "Add src/pages/share-session.identity.test.tsx covering (a) identity is persisted on join, (b) identity is reused on reload, (c) entries older than 24h are discarded",
        "`bun test src/pages/share-session.identity.test.tsx` passes",
        "Add apps/studio/src/share.host-identity.test.ts: assert hostDisplayName resolves to os.userInfo().username and is included in `state()` snapshot",
        "`bun test apps/studio/src/share.host-identity.test.ts` passes",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 10,
      "passes": false,
      "notes": ""
    }
  ]
}