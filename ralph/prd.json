{
  "project": "SeeFlow Canvas",
  "branchName": "ralph/canvas-sidebar-autofit-headerbg",
  "description": "Three @seeflow/canvas improvements bundled: relocate the studio's DetailPanel into the canvas package and render it internally via SeeflowCanvas, add an opt-in autoFitView (initial mount + external-source node changes via a host-bumped signal), and replace bg-muted/30 with a per-token headerBackground field so node headers read as clearly distinct on all 8 color tokens. See docs/plans/2026-05-18-canvas-sidebar-autofit-headerbg-design.md for full design.",
  "userStories": [
    {
      "id": "US-001",
      "title": "Add per-token headerBackground to color tokens",
      "description": "As a canvas consumer, I want each node color token to define its own header background so headers stay visually distinct on every body color.",
      "acceptanceCriteria": [
        "Extend COLOR_TOKEN_MAP in packages/canvas/src/lib/color-tokens.ts: each entry gains a headerBackground field. default = 'hsl(var(--muted))'. slate/blue/green/amber/red/purple/pink each get an HSL ~4 lightness points darker than their body background, same hue/saturation",
        "Add a 'node-header' kind to colorTokenStyle returning NodeHeaderColorStyle = Pick<CSSProperties, 'backgroundColor'>",
        "Export NodeHeaderColorStyle from packages/canvas/src/index.ts in the same section as the existing color style type exports",
        "Add unit tests in packages/canvas/src/lib/color-tokens.test.ts (create the file if missing) covering colorTokenStyle(undefined, 'node-header') and at least one non-default token",
        "Typecheck passes (cd packages/canvas && bun run typecheck) and root bun test passes",
        "bun run format then bun run lint pass with no errors"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-002",
      "title": "Apply headerBackground to shape-node and state-node headers",
      "description": "As a user, I want every node header to render in its token's header color instead of a constant bg-muted/30 overlay so the header reads clearly on colored nodes.",
      "acceptanceCriteria": [
        "In packages/canvas/src/nodes/shape-node.tsx replace 'bg-muted/30' on the header div (data-testid='shape-node-header') with an inline style from colorTokenStyle(data.color, 'node-header'). Keep border-b, padding, flex classes",
        "In packages/canvas/src/nodes/state-node.tsx make the same change on the header div (data-testid='node-header')",
        "Update packages/canvas/src/nodes/shape-node.test.tsx so header assertions check the inline backgroundColor style instead of the bg-muted/30 class",
        "Update packages/canvas/src/nodes/state-node.tsx tests similarly if any assert the old class",
        "Typecheck passes and bun test passes",
        "bun run format then bun run lint pass",
        "Verify in browser using dev-browser skill: bun run dev, open a demo with nodes of several different color tokens, confirm headers read as a clearly darker shade on every token"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-003",
      "title": "Add openFile and revealFile to CanvasAdapter",
      "description": "As a canvas consumer, I want optional file-system actions on the adapter so the sidebar can open and reveal project files without hardcoding the studio's REST routes.",
      "acceptanceCriteria": [
        "Add two optional methods to the CanvasAdapter interface in packages/canvas/src/adapter/types.ts: openFile?(path: string): Promise<void> and revealFile?(path: string): Promise<void>. Document each with a one-line JSDoc",
        "Implement both in createRestAdapter in packages/canvas/src/adapter/rest.ts. Confirm the exact endpoint paths and request shape by reading apps/web/src/lib/api.ts (openProjectFile and revealProjectFile). The adapter may need a new projectId option — add it to RestAdapterOptions if so",
        "If RestAdapterOptions gains projectId, update apps/web/src/pages/demo-view.tsx (or wherever createRestAdapter is called) to pass it",
        "Add tests in packages/canvas/src/adapter/rest.test.ts (create if missing) covering openFile and revealFile: assert the URL, method, and body of the fetch call",
        "Typecheck passes and bun test passes",
        "bun run format then bun run lint pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-004",
      "title": "Add peer deps and export DetailPanel-related types",
      "description": "As a canvas consumer, I want react-markdown, remark-gfm, and react-dom declared as peer dependencies so the package's status section markdown rendering works after the sidebar relocates.",
      "acceptanceCriteria": [
        "Add to peerDependencies in packages/canvas/package.json: 'react-dom': '*', 'react-markdown': '^10', 'remark-gfm': '^4'",
        "Add a one-line note under the README's 'Peer dependencies' section: react-markdown and remark-gfm are required for the built-in sidebar's status section",
        "Do NOT yet move DetailPanel — this story is package.json + README only",
        "bun install runs cleanly from repo root with no missing peer dep warnings for the new entries",
        "Typecheck passes and bun test passes",
        "bun run format then bun run lint pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-005",
      "title": "Relocate detail-panel.tsx into @seeflow/canvas",
      "description": "As a maintainer, I want the studio's DetailPanel to live inside the canvas package so the sidebar ships with the canvas API instead of being composed at the app layer.",
      "acceptanceCriteria": [
        "git mv apps/web/src/components/detail-panel.tsx to packages/canvas/src/components/detail-panel.tsx (preserves blame)",
        "Inside the relocated file: replace `from '@seeflow/canvas'` and `from '@/lib/api'` imports with relative paths. Types DemoNode / Connector / StatusReport come from '../types.ts'. UI primitives (Button, Sheet*, StatusBadge) and helpers (cn, getStoredDetailPanelWidth, setStoredDetailPanelWidth, startResizeGesture) come from their actual source files under '../ui/' or '../lib/'",
        "Replace openProjectFile(projectId, path) / revealProjectFile(projectId, path) call sites with adapter.openFile?.(path) / adapter.revealFile?.(path). Add an `adapter` prop to DetailPanelProps (typed CanvasAdapter | null/undefined). Hide the Open / Reveal buttons when the corresponding adapter method is undefined",
        "Export DetailPanel and DetailPanelProps from packages/canvas/src/index.ts in the existing Chrome section, keeping the section's alphabetical order",
        "Update apps/web/src/pages/demo-view.tsx so its DetailPanel import resolves: `import { DetailPanel } from '@seeflow/canvas'`. Pass the existing adapter into the DetailPanel `adapter` prop",
        "Leave apps/web/src/components/detail-panel.test.tsx in place but update its import to `from '@seeflow/canvas'` so it keeps testing the (relocated) component",
        "Typecheck passes and bun test passes (existing DetailPanel tests must continue to pass against the relocated component)",
        "bun run format then bun run lint pass",
        "Verify in browser using dev-browser skill: bun run dev, open a demo, click a node, the sidebar opens and edits/file actions still work end-to-end"
      ],
      "priority": 5,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-006",
      "title": "Relocate detail-panel.test.tsx into @seeflow/canvas",
      "description": "As a maintainer, I want the DetailPanel test file to live next to its source inside the canvas package per the workspace convention (foo.ts + foo.test.ts side by side).",
      "acceptanceCriteria": [
        "git mv apps/web/src/components/detail-panel.test.tsx to packages/canvas/src/components/detail-panel.test.tsx",
        "Rewire imports: DemoNode comes from '../types.ts'; DetailPanel comes from './detail-panel.tsx'; any other `@/` paths become relative",
        "Any usage of `openProjectFile` / `revealProjectFile` (if asserted) becomes a fake adapter with openFile / revealFile method spies",
        "Typecheck passes and bun test passes (DetailPanel test suite still runs and still passes)",
        "bun run format then bun run lint pass"
      ],
      "priority": 6,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-007",
      "title": "Render DetailPanel inside SeeflowCanvas and migrate demo-view",
      "description": "As a host app developer, I want SeeflowCanvas to render the sidebar internally so I no longer compose a sibling DetailPanel and resize handle in my layout.",
      "acceptanceCriteria": [
        "Add to SeeflowCanvasProps in packages/canvas/src/components/seeflow-canvas.tsx: disableSidebar?: boolean (default false); statusReport?: StatusReport & { ts: number }; onNameChange / onDescriptionChange / onDetailChange field-edit callbacks matching DetailPanelProps signatures",
        "Render <DetailPanel /> inside the canvas's outer flex layout (right side). Pass adapter, selected node/connector (looked up from selectedNodeIds[0] / selectedConnectorIds[0] in the existing nodes/connectors props), the new callbacks, and statusReport. Suppress rendering entirely when disableSidebar is true",
        "Move the width-resize handle JSX (the divider that drives startResizeGesture + getStoredDetailPanelWidth / setStoredDetailPanelWidth) from apps/web/src/pages/demo-view.tsx into the canvas component. Keep DETAIL_PANEL_WIDTH_KEY unchanged so existing widths persist",
        "In apps/web/src/pages/demo-view.tsx: remove the <DetailPanel> JSX, the resize-handle JSX, and the unused imports. Pass the existing callbacks (onNameChange, onDescriptionChange, onDetailChange, statusByNode[selectedNodeId]) into <SeeflowCanvas>",
        "Add tests in packages/canvas/src/components/seeflow-canvas.test.tsx covering: sidebar renders when a node is selected; sidebar hidden when disableSidebar=true; field-edit callbacks fire on user edits",
        "Typecheck passes and bun test passes (no regressions in seeflow-canvas, detail-panel, or demo-view test suites)",
        "bun run format then bun run lint pass",
        "Verify in browser using dev-browser skill: bun run dev, open a demo, sidebar still opens on selection, resize handle still works, edits still persist to the backend"
      ],
      "priority": 7,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-008",
      "title": "Add autoFitView prop with mount-fit effect",
      "description": "As a canvas consumer, I want an opt-in autoFitView prop that frames the flow once on initial mount so a freshly-loaded demo lands at a sensible zoom without a manual click.",
      "acceptanceCriteria": [
        "Add to SeeflowCanvasProps: autoFitView?: boolean | { onMount?: boolean; onExternalNodeChange?: boolean }. Default behavior: autoFitView undefined or false → no auto-fit. autoFitView=true → both onMount and onExternalNodeChange default to true",
        "Define a single module-level FIT_VIEW_OPTIONS constant = { padding: 0.15, duration: 300, includeHiddenNodes: false }. Replace the manual Fit View button's inline options with this constant so the two paths cannot drift",
        "Add a useEffect (guarded by a didMountFitRef) that runs once after rfInstanceRef.current is initialized AND nodes.length > 0 AND the resolved onMount flag is true. Calls rfInstanceRef.current.fitView(FIT_VIEW_OPTIONS)",
        "Add tests in seeflow-canvas.test.tsx: (a) autoFitView default → no fitView call on mount; (b) autoFitView=true with nodes → exactly one fitView call with FIT_VIEW_OPTIONS; (c) autoFitView=true with nodes.length=0 → no call; (d) autoFitView={{ onMount: false }} → no call",
        "External-change trigger (autoFitViewSignal) is NOT implemented in this story — only mount-fit and the FIT_VIEW_OPTIONS centralization",
        "Typecheck passes and bun test passes",
        "bun run format then bun run lint pass"
      ],
      "priority": 8,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-009",
      "title": "Add autoFitViewSignal with interaction-guarded external-change fit",
      "description": "As a canvas consumer, I want a host-bumped signal counter that triggers fitView on external node changes (SSE, adapter) without yanking the user mid-drag or mid-resize.",
      "acceptanceCriteria": [
        "Add to SeeflowCanvasProps: autoFitViewSignal?: number. Host increments this when nodes change from an external source",
        "Add a useEffect keyed on [autoFitViewSignal]. When the signal increments AND the resolved onExternalNodeChange flag is true: call rfInstanceRef.current.fitView(FIT_VIEW_OPTIONS). Skip the very first run (initial signal value) so it does not double-fire with the mount-fit effect",
        "Interaction guard: if the trigger fires while isResizing is true OR React Flow reports an in-progress node drag, store the intent in a pendingFitRef and DO NOT call fitView. When the interaction ends (isResizing flips false; drag completes), flush the pending fit once",
        "Add tests in seeflow-canvas.test.tsx: (a) bumping autoFitViewSignal triggers exactly one fitView; (b) bumping while isResizing=true defers the call; clearing isResizing flushes exactly one fitView; (c) autoFitView={{ onExternalNodeChange: false }} → bumping the signal does NOT fit",
        "Typecheck passes and bun test passes",
        "bun run format then bun run lint pass"
      ],
      "priority": 9,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-010",
      "title": "Wire studio to bump autoFitViewSignal on SSE node events",
      "description": "As a studio user, I want the canvas to re-frame the flow automatically when new nodes arrive from the backend so I do not lose track of where the action is.",
      "acceptanceCriteria": [
        "In apps/web/src/pages/demo-view.tsx: add a useState<number>(0) for autoFitViewSignal",
        "In the SSE handler that processes node:created and node:deleted events (find the existing hook/effect that updates the nodes state in response to SSE), increment the autoFitViewSignal counter alongside the existing state update",
        "Pass autoFitView={true} and autoFitViewSignal={autoFitViewSignal} into <SeeflowCanvas>",
        "Local user creates (drag-from-handle, paste, drag-create-shape) must NOT bump the signal — only external SSE-driven updates",
        "Typecheck passes and bun test passes",
        "bun run format then bun run lint pass",
        "Verify in browser using dev-browser skill: bun run dev, open a demo, run /seeflow to generate a new node via the plugin (or trigger any SSE node:created path), confirm the canvas auto-fits when the node appears, and confirm dragging a shape from the toolbar onto the canvas does NOT yank the viewport"
      ],
      "priority": 10,
      "passes": false,
      "notes": ""
    },
    {
      "id": "US-011",
      "title": "Merge ralph/canvas-sidebar-autofit-headerbg into main and push",
      "description": "As the project owner, I want the completed feature branch merged into main and pushed to origin so the work ships once all preceding stories pass.",
      "acceptanceCriteria": [
        "Pre-flight gate: confirm every preceding user story (US-001 through US-010) has passes=true in ralph/prd.json. If ANY story still has passes=false, abort this task with a clear message and do NOT touch git",
        "Run the full verification suite from repo root before merging: bun run typecheck, bun test, bun run format, bun run lint. ALL must pass. If any fail, abort and do NOT merge",
        "Confirm the working tree is clean (git status --porcelain produces no output). If dirty, abort — do NOT stash, do NOT commit",
        "git fetch origin then git checkout main and git pull --ff-only origin main to ensure main is up to date",
        "git merge --no-ff ralph/canvas-sidebar-autofit-headerbg with a merge commit message: 'feat(canvas): sidebar incorporation + auto-fit-view + per-token header bg' followed by a short body summarizing the three improvements",
        "Re-run bun run typecheck and bun test on the merged main to catch any merge-resolution regressions. If either fails, do NOT push — leave main locally merged so the user can inspect, and report the failure",
        "git push origin main (regular push only — NEVER --force or --force-with-lease)",
        "Report the merge commit SHA and the pushed branch state at the end"
      ],
      "priority": 11,
      "passes": false,
      "notes": ""
    }
  ]
}
