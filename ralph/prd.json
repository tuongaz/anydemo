{
  "project": "SeeFlow Live Share",
  "branchName": "ralph/live-share-phase-3",
  "description": "Phase 3 \u2014 peer SPA route + share-client.ts (read-only viewing). Implementation: /Users/tuongaz/dev/seeflow-viewer/src/. Design: docs/plans/2026-05-31-live-share-design.md (Peer SPA).",
  "userStories": [
    {
      "id": "US-025",
      "title": "Add Bun test runner and Zod-based share envelope deps to seeflow-viewer",
      "description": "As a maintainer, I need bun test wired into the seeflow-viewer repo so subsequent share-client stories can land unit tests, and the Zod dep upgraded if needed for the envelope schema.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Add `\"test\": \"bun test\"` to scripts in /Users/tuongaz/dev/seeflow-viewer/package.json (currently has typecheck/dev/build/preview only \u2014 see /Users/tuongaz/dev/seeflow-viewer/CLAUDE.md note 'No test suite is configured')",
        "Confirm `zod@^3.23.8` is already in dependencies (it is at line 26 of package.json); do not bump",
        "Add devDependency `@types/bun` at the version compatible with the installed bun (read installed version via `bun --version` and pin matching @types/bun)",
        "Add a trivial smoke test at /Users/tuongaz/dev/seeflow-viewer/src/lib/share-test-harness.test.ts that imports `expect, test` from 'bun:test' and asserts `expect(1 + 1).toBe(2)` \u2014 purely to prove `bun test` is wired",
        "Update /Users/tuongaz/dev/seeflow-viewer/tsconfig.json `types` array to include `\"bun-types\"` (or keep `@types/bun` via devDependency in compilerOptions.types as appropriate) so `bun:test` imports typecheck",
        "Run `bun install`; commit any updated /Users/tuongaz/dev/seeflow-viewer/bun.lock",
        "`bun test src/lib/share-test-harness.test.ts` exits 0 with 1 pass",
        "`bun run typecheck` passes",
        "`bun run build` still succeeds (Vite must not pick up *.test.ts in the production bundle \u2014 confirm by searching dist/ for the test file name; if Vite includes it, add a Vite `build.rollupOptions.input` glob exclusion or rely on Vite default which only entry-points index.html)",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 1,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-026",
      "title": "Add share envelope Zod schema + parser to seeflow-viewer",
      "description": "As share-client.ts, I need a Zod schema that mirrors the relay's WebSocket envelope so incoming frames can be parsed and discriminated by type with no `any`.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Create /Users/tuongaz/dev/seeflow-viewer/src/lib/share-envelope.ts exporting `EnvelopeSchema` (zod.object) matching the contract in /Users/tuongaz/dev/seeflow/docs/plans/2026-05-31-live-share-design.md lines 107-122: `v: z.literal(1)`, `type` enum (`auth-host`, `auth-peer`, `rpc`, `rpc-result`, `sse`, `presence`, `file-request`, `file-bytes`, `file-redirect`, `file-upload-intent`, `file-upload-done`, `node-patched`, `files-manifest`, `kick`, plus phase-3 additions `flow-snapshot` and `auth-refresh-required`), `id?: string`, `from: string`, `to?: string | 'host' | 'all'`, `payload: z.unknown()`",
        "Export `type Envelope = z.infer<typeof EnvelopeSchema>`",
        "Export `parseEnvelope(raw: unknown): { ok: true; envelope: Envelope } | { ok: false; reason: string }` (mirrors cloud/lambda/share/shared/envelope.ts)",
        "Create /Users/tuongaz/dev/seeflow-viewer/src/lib/share-envelope.test.ts with at least 5 cases: valid `rpc-result` frame, valid `flow-snapshot` frame, valid `node-patched` frame, wrong `v: 2` rejected, unknown `type: 'banana'` rejected",
        "`bun test src/lib/share-envelope.test.ts` shows 5 pass",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 2,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-027",
      "title": "Implement share-client.ts WebSocket connect + auth-peer handshake",
      "description": "As the share-session page, I need a ShareClient class that opens a WebSocket, sends the auth-peer frame, resolves on the relay's auth ack, and exposes a connection-state event stream.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Create /Users/tuongaz/dev/seeflow-viewer/src/lib/share-client.ts exporting `ShareClient` class with constructor `(opts: { wsUrl: string; sessionId: string; peerJwt: string; displayName: string; webSocketImpl?: typeof WebSocket })` \u2014 `webSocketImpl` exists solely for test injection and defaults to globalThis.WebSocket",
        "Public API: `connect(): Promise<void>` (resolves on auth ack, rejects on close before ack), `disconnect(reason?: string): void`, `on(event: 'state', listener: (state: ConnectionState) => void): () => void` returning unsubscribe, `state: ConnectionState` getter",
        "`ConnectionState` discriminated union: `{ kind: 'idle' } | { kind: 'connecting' } | { kind: 'authed' } | { kind: 'closed'; code: number; reason: string }`",
        "On `ws.onopen`, send a single JSON-stringified envelope `{ v: 1, type: 'auth-peer', from: 'peer', payload: { peerJwt, displayName } }` per /Users/tuongaz/dev/seeflow/docs/plans/2026-05-31-live-share-design.md line 148",
        "Treat auth as acknowledged when the relay either echoes/forwards any non-error frame within 5 seconds OR when 250ms elapse with no close (relay does not currently send an explicit auth-ack \u2014 see ws-default.ts: 200 reply is HTTP-only); use the 250ms quiet-window approach so a missing ack does not block",
        "On close before auth, reject `connect()` with an Error whose message includes the close code and reason",
        "Create /Users/tuongaz/dev/seeflow-viewer/src/lib/share-client.test.ts using a fake WebSocket constructor injected via `webSocketImpl`; at minimum cover: (a) sends auth-peer as the first frame on open with correct shape, (b) state transitions idle->connecting->authed, (c) connect() rejects when the fake fires `onclose({ code: 1008, reason: 'unauthorized' })` before open",
        "All test WebSocket interactions must go through the injected fake \u2014 no real network",
        "`bun test src/lib/share-client.test.ts` shows >=3 pass",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 3,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-028",
      "title": "Add incoming frame dispatch + typed listener API to share-client.ts",
      "description": "As the share-session page, I need share-client to parse every incoming frame through the envelope Zod schema, drop invalid frames, and dispatch valid ones to per-type listeners so canvas + UI can react without touching the raw WebSocket.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Extend /Users/tuongaz/dev/seeflow-viewer/src/lib/share-client.ts with `on(event: 'frame', listener: (envelope: Envelope) => void): () => void` plus type-narrowed shortcuts: `on('flow-snapshot', listener)`, `on('node-patched', listener)`, `on('files-manifest', listener)`, `on('kick', listener)`, `on('auth-refresh-required', listener)` \u2014 all return unsubscribe functions",
        "Every `ws.onmessage` invocation: `JSON.parse` the data, call `parseEnvelope` from /Users/tuongaz/dev/seeflow-viewer/src/lib/share-envelope.ts, drop+`console.warn('share: invalid envelope', reason)` on failure, otherwise call listeners for `'frame'` and listeners for the specific `envelope.type`",
        "Add `kicked: { peerId: string; reason?: string } | null` to `ConnectionState` via a new `{ kind: 'kicked'; peerId: string; reason?: string }` variant, transitioned to when a `kick` frame addressed `to: <thisPeerId>` (or `to: 'all'`) is received \u2014 close the WS afterwards with code 1000",
        "Listener exceptions must not crash the client \u2014 wrap each call in try/catch and console.error",
        "Extend /Users/tuongaz/dev/seeflow-viewer/src/lib/share-client.test.ts with cases: (a) malformed JSON message is dropped without throwing, (b) a `flow-snapshot` frame triggers only the `flow-snapshot` listener AND the generic `frame` listener, (c) a `kick` frame transitions state to `{ kind: 'kicked', ... }` and closes the socket",
        "`bun test src/lib/share-client.test.ts` shows >=6 pass (3 from US-027 + 3 new)",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 4,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-029",
      "title": "Add /share/:token route and ShareSession page shell",
      "description": "As a peer, I want navigating to share.seeflow.dev/<token> to render a page shell with the dark ViewerHeader chrome so I have visible feedback the link routed correctly before any network call.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Edit /Users/tuongaz/dev/seeflow-viewer/src/app.tsx: add `<Route path=\"/share/:token\" element={<ShareSession />} />` inside the existing `<Route element={<ViewerLayout />}>` block (so it inherits ViewerHeader) \u2014 import from `./pages/share-session.tsx`",
        "Create /Users/tuongaz/dev/seeflow-viewer/src/pages/share-session.tsx exporting `ShareSession()` that reads `token` from `useParams<{ token: string }>()`",
        "ShareSession initial render: a centred container using the same `centred` flex pattern from /Users/tuongaz/dev/seeflow-viewer/src/pages/flow-view.tsx, with a heading 'Live Share' (Inter, --text), eyebrow 'SESSION' (JetBrains Mono, --text-mute, uppercase, letter-spacing 0.18em), and a status line showing the token (truncated to first 8 chars + ellipsis) \u2014 purely static at this story",
        "If `token` is missing/empty, render the same 404 block used in /Users/tuongaz/dev/seeflow-viewer/src/pages/flow-view.tsx lines 75-95 with body text 'Invalid share link'",
        "ViewerHeader does not need branch logic for this route (it shows just the SeeFlow logo + theme cog by default since `isFlowView` and `isProjectView` are both false)",
        "`bun run typecheck` passes",
        "`bun run build` succeeds with the new route in the bundle",
        "Manual verify: `bun run dev` then visit http://localhost:5173/share/test-token-1234567 renders the 'Live Share' shell with no console errors (record screenshot in commit message)",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 5,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-030",
      "title": "Add DisplayNameModal component and gate ShareSession on it",
      "description": "As a first-time peer, I want a modal asking for my display name before joining so my edits/cursors carry attribution per the design doc.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Create /Users/tuongaz/dev/seeflow-viewer/src/components/display-name-modal.tsx exporting `DisplayNameModal({ onSubmit }: { onSubmit: (name: string) => void })`",
        "Renders a centred modal (use existing `Popover`/`Command` pattern is not required \u2014 a plain fixed-position overlay backed by `var(--bg)` at 60% opacity + a `--surface` card is fine) with: eyebrow 'DISPLAY NAME' (JetBrains Mono, --text-mute, uppercase, letter-spacing 0.18em), `<input type=\"text\">` styled per design.html (`--surface`, 1px `--border`, focus-ring `--emerald`), and an emerald 'Continue' submit button",
        "Trim + cap input at 40 chars before calling `onSubmit` (matches /Users/tuongaz/dev/seeflow-viewer/cloud/lambda/share/session-join.ts line ~62)",
        "Disable the submit button when trimmed name is empty",
        "Submit on Enter key as well as button click",
        "Persist accepted name to `localStorage.setItem('seeflow.share.displayName', name)` and read it as the default value on mount (do NOT auto-submit \u2014 peer can edit and resubmit)",
        "Edit /Users/tuongaz/dev/seeflow-viewer/src/pages/share-session.tsx: maintain local state `displayName: string | null`; if null, render `<DisplayNameModal onSubmit={setDisplayName} />` instead of the join flow; once set, render the existing shell from US-029 with the display name in the status line",
        "`bun run typecheck` passes",
        "`bun run build` succeeds",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 6,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-031",
      "title": "Implement joinShare() helper and wire it into ShareSession",
      "description": "As ShareSession, I need a typed wrapper around POST /api/share/join so the page can resolve { sessionId, wsUrl, peerJwt, flowList, hostOnline } from a token + displayName.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Add to /Users/tuongaz/dev/seeflow-viewer/src/lib/viewer-api.ts (or a new /Users/tuongaz/dev/seeflow-viewer/src/lib/share-api.ts) the following exported types + function:",
        "- `interface ShareJoinResponse { sessionId: string; peerId: string; displayName: string; wsUrl: string; peerJwt: string; hostOnline: boolean; flowList: Array<{ id: string; name: string; icon?: string }> }`",
        "- `class ShareTokenError extends Error` with `name = 'ShareTokenError'`",
        "- `async function joinShare(args: { token: string; displayName: string; signal?: AbortSignal; baseUrl?: string }): Promise<ShareJoinResponse>` \u2014 POSTs JSON body `{ token, displayName }` to `${baseUrl ?? 'https://seeflow.dev'}/api/share/join`",
        "On HTTP 404, throw `ShareTokenError('unknown-token')`; on HTTP 400 throw `ShareTokenError(body.error ?? 'bad-request')`; on other !ok, throw plain Error with status",
        "Edit /Users/tuongaz/dev/seeflow-viewer/src/pages/share-session.tsx: add `joinState: { status: 'idle' } | { status: 'joining' } | { status: 'joined'; data: ShareJoinResponse } | { status: 'error'; message: string; revoked?: boolean }` state machine; on displayName set, transition to 'joining' and call `joinShare({ token, displayName })`; on success render the current shell with the response payload (peer-id and flowList visible as a placeholder list) \u2014 actual canvas mount lands in US-033",
        "On `ShareTokenError('unknown-token')`, render 'This share link is no longer active' per /Users/tuongaz/dev/seeflow/docs/plans/2026-05-31-live-share-design.md line 215, styled like the 404 block",
        "Abort the in-flight fetch on unmount via AbortController",
        "Add /Users/tuongaz/dev/seeflow-viewer/src/lib/share-api.test.ts (or extend viewer-api.test.ts if you create that) with at least 3 cases using a `globalThis.fetch` mock: happy path, 404 -> ShareTokenError, 400 -> ShareTokenError",
        "`bun test src/lib/share-api.test.ts` shows >=3 pass",
        "`bun run typecheck` passes",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 7,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-032",
      "title": "Connect ShareSession to ShareClient and handle host-offline / disconnected states",
      "description": "As a peer, after joining I want the WebSocket to connect and the page to clearly distinguish 'waiting for host snapshot', 'host offline', and 'disconnected' states.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Edit /Users/tuongaz/dev/seeflow-viewer/src/pages/share-session.tsx: on `joinState.status === 'joined'`, instantiate `new ShareClient({ wsUrl, sessionId, peerJwt, displayName })` from /Users/tuongaz/dev/seeflow-viewer/src/lib/share-client.ts and call `.connect()` inside a useEffect; clean up via `.disconnect('unmount')` on unmount",
        "Add `wsState: ConnectionState` mirrored into React state via the client's `on('state', ...)` subscription",
        "If `joinResponse.hostOnline === false`, render a 'Host stopped sharing' card (eyebrow 'OFFLINE', body copy 'The host is no longer connected. Try again in a moment.', emerald 'Retry' button that re-runs `joinShare` per /Users/tuongaz/dev/seeflow/docs/plans/2026-05-31-live-share-design.md line 213)",
        "If `wsState.kind === 'closed'` after auth, render a 'Disconnected from host' card with the same Retry pattern (re-run joinShare + new ShareClient)",
        "If `wsState.kind === 'kicked'`, render 'You were removed from this session' per design doc line 214 \u2014 Retry button is hidden",
        "If `wsState.kind === 'connecting'` or `'authed'` but no snapshot yet (covered in US-033), show a centred spinner using the existing `.spinner` class + 'Connecting\u2026' label",
        "Manually verify against a stubbed WS server in a follow-up; for now ensure the state-machine branches typecheck and the file builds",
        "`bun run typecheck` passes",
        "`bun run build` succeeds",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 8,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-033",
      "title": "Render read-only canvas from flow-snapshot frame in ShareSession",
      "description": "As a peer, once the WebSocket is authed and the host has sent the initial flow snapshot, I want the canvas to render in view-only mode showing the host's flow.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Document the expected snapshot frame shape inline in /Users/tuongaz/dev/seeflow-viewer/src/lib/share-client.ts as a TS interface `FlowSnapshotPayload { flows: Record<string, Flow>; activeFlowId: string }` (mirrors host responsibility from /Users/tuongaz/dev/seeflow/docs/plans/2026-05-31-live-share-design.md line 284 'host sends a full-state snapshot' \u2014 host implementation lives in phase 2; this story defines the contract the peer consumes)",
        "Validate snapshot payload via a Zod schema `FlowSnapshotSchema` colocated in /Users/tuongaz/dev/seeflow-viewer/src/lib/share-client.ts (or a sibling file); reject snapshots that don't parse",
        "Edit /Users/tuongaz/dev/seeflow-viewer/src/pages/share-session.tsx: subscribe via `shareClient.on('flow-snapshot', ...)`; store `snapshot: FlowSnapshotPayload | null` in state; once non-null, render `<ViewCanvas flow={snapshot.flows[activeFlowId]} uuid={sessionId} enableShare={false} canvasProjectId={sessionId} fileBaseUrl={\\`/api/share/${sessionId}/files\\`} />` from /Users/tuongaz/dev/seeflow-viewer/src/components/view-canvas.tsx",
        "Use `normalizeFlow`-equivalent fallback: if snapshot flow has nodes without `position`, run the same layout normalisation already in /Users/tuongaz/dev/seeflow-viewer/src/lib/viewer-api.ts `withNodePositions` \u2014 export it from viewer-api.ts and reuse, do not duplicate",
        "Wire `node-patched` frames to update the current snapshot's flow in state (apply diff at top level \u2014 for phase 3 a wholesale node replace by id is acceptable; deep diff lands in phase 4). Frame payload shape: `{ flowId: string; nodeId: string; node: FlowNode | null /* null = delete */ }`",
        "Until the first `flow-snapshot` arrives, render the 'Connecting\u2026' spinner from US-032",
        "`bun run typecheck` passes",
        "`bun run build` succeeds and the resulting dist bundle includes the ShareSession code (verify with `grep -l 'flow-snapshot' dist/assets/*.js`)",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 9,
      "passes": true,
      "notes": ""
    },
    {
      "id": "US-034",
      "title": "Drive FlowSwitcher from join.flowList and support active-flow switching in ShareSession",
      "description": "As a peer in a multi-flow shared project, I want the FlowSwitcher to list every flow the host shared so I can hop between them without re-joining.",
      "acceptanceCriteria": [
        "Working dir: /Users/tuongaz/dev/seeflow-viewer",
        "Edit /Users/tuongaz/dev/seeflow-viewer/src/pages/share-session.tsx: feed the existing /Users/tuongaz/dev/seeflow-viewer/src/components/flow-switcher.tsx (`FlowSwitcher`) via `ViewCanvas`'s `topRightSlot` prop using `flowList` from the join response; pass `defaultFlow={snapshot.activeFlowId}` and `active={localActiveFlowId}` where `localActiveFlowId` defaults to `snapshot.activeFlowId`",
        "On switcher change, set `localActiveFlowId` and re-render `<ViewCanvas flow={snapshot.flows[localActiveFlowId]} ... canvasProjectId={localActiveFlowId} />`; do NOT mutate the URL (share session URL stays `/share/:token`)",
        "If `flowList.length <= 1`, omit the slot entirely (FlowSwitcher already returns null in that case \u2014 but skip even constructing it to avoid the empty React node)",
        "If `snapshot.flows[localActiveFlowId]` is undefined (host pruned a flow mid-session), fall back to `snapshot.activeFlowId` and warn via console",
        "Add a `useEffect` that resets `localActiveFlowId` to the new `snapshot.activeFlowId` when the snapshot id changes (e.g. after a reconnect that delivers a new snapshot) \u2014 but ONLY if the user has not explicitly changed it since the last snapshot (track via a `userPickedFlow` boolean ref)",
        "`bun run typecheck` passes",
        "`bun run build` succeeds",
        "Manual verify (note: requires a stubbed host until phase 2 lands): `bun run dev`, hit `/share/<token>` with a hand-crafted local mock of joinShare returning a 2-flow `flowList`, confirm FlowSwitcher appears in the canvas top-right and switching updates the rendered flow",
        "Typecheck passes",
        "Tests pass"
      ],
      "priority": 10,
      "passes": true,
      "notes": ""
    }
  ]
}