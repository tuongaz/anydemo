# Ralph Progress Log
Started: Sun 31 May 2026 20:53:58 AEST
---

## Codebase Patterns
- Phase 5 spans two repos: seeflow (this worktree) and seeflow-viewer (separate clone at /Users/tuongaz/dev/seeflow-viewer). Each story's `Working dir:` line in the acceptance criteria dictates which repo to edit. Commit in the repo you edited.
- Cloud relay code lives at /Users/tuongaz/dev/seeflow-viewer/cloud on branch `ralph/live-share-phase-5` (created from phase-3 for this phase).
- Envelope frame types are gated by a Zod string-enum in cloud/lambda/share/shared/envelope.ts; payload Zod schemas live in the same module and are imported by per-route handlers.
- Use `bun test <relative path>` from the package root (not `bun test packages/...`); each repo uses Bun's native test runner.
- Cloud relay tests mock both DynamoDB (`(ddbMod.doc as any).send = fakeDocSend`) and ApiGatewayManagement (`(ApiGatewayManagementApiClient.prototype as any).send = fakeApiSend`). Reuse the pattern from `ws-default.test.ts` whenever a handler now needs to broadcast.
- Shared lambda helpers belong under `cloud/lambda/share/shared/` — `ws-api.ts` (apiClient + post with GoneException handling) is reusable across any handler that fans out frames.
- The seeflow repo is a git worktree setup — the active worktree for Phase 5 work in seeflow lives at `/Users/tuongaz/dev/seeflow/.claude/worktrees/realtime-share-design-doc` on branch `worktree-realtime-share-design-doc`. ALWAYS edit + commit there, NOT in the main repo at `/Users/tuongaz/dev/seeflow` (which is on `main`). Absolute paths to `/Users/tuongaz/dev/seeflow/packages/...` resolve to the main repo, not the worktree — use the worktree's absolute path explicitly.
- Pre-existing failing tests in `apps/studio/src/{watcher,server,registry}.test.ts` (fs-watcher + HTTP timeouts) are NOT caused by canvas-only work; verify canvas-scope changes via `bun test packages/canvas` (currently 1272 pass) rather than full `bun test`.

---

## 2026-05-31 - US-045
- Added 'cursor' to EnvelopeSchema type enum alongside existing 'presence' literal.
- Added PresencePayloadSchema (peerId, displayName<=40, hex color, state enum, optional flowId) and CursorPayloadSchema (peerId, flowId, finite x/y, optional viewport, non-negative int ts), plus their inferred types.
- Extended envelope.test.ts with valid-cursor, displayName-overflow, malformed-hex, and NaN-coord cases. 11 tests pass; `bun run typecheck` clean.
- Files changed:
  - cloud/lambda/share/shared/envelope.ts
  - cloud/lambda/share/shared/envelope.test.ts
- **Learnings for future iterations:**
  - The cloud repo had no phase-5 branch yet — I branched from `ralph/live-share-phase-3` (the latest in-flight branch) rather than `main`, since phase 5 depends on prior phases' relay/handler scaffolding.
  - `parseEnvelope` validates only the envelope shape; payload schemas (Presence/Cursor) must be applied separately by route handlers via `.safeParse`.
  - Zod's `z.number().finite()` rejects NaN/Infinity — preferred over manual checks.
  - Test imports must come from the same module that exports the schemas; both `PresencePayloadSchema` and `CursorPayloadSchema` are now top-level exports.
---

## 2026-05-31 - US-046
- Server-side deterministic peer color (sha256 → HSL hue → hex) added in `cloud/lambda/share/shared/peer-color.ts`; persisted on `PeerInfo` via `addPeer`.
- ws-default `handleAuthPeer`: after successful `addPeer`, fans out a `state:'joined'` presence envelope to host + existing peers (sender excluded) with peerId/displayName/color triple. GoneException recipients are cleaned up.
- ws-disconnect: broadcasts `state:'left'` to host + remaining peers on peer disconnect; broadcasts `state:'host-offline'` (fixed gray) to remaining peers on host disconnect.
- `PresencePayloadSchema` state enum extended with `'host-offline'`.
- Factored `apiClient` + `post` into `cloud/lambda/share/shared/ws-api.ts` so ws-default and ws-disconnect share identical fan-out + GoneException semantics.
- New `ws-default.presence.test.ts` (2 tests) covers happy-path fan-out and gone-recipient continuation. `ws-disconnect.test.ts` rewritten to mock the API client and assert left/host-offline broadcasts.
- Full cloud suite: 90 tests / 0 fail / typecheck clean.
- Files changed:
  - cloud/lambda/share/shared/envelope.ts
  - cloud/lambda/share/shared/sessions.ts
  - cloud/lambda/share/shared/sessions.test.ts
  - cloud/lambda/share/shared/peer-color.ts (new)
  - cloud/lambda/share/shared/ws-api.ts (new)
  - cloud/lambda/share/ws-default.ts
  - cloud/lambda/share/ws-default.test.ts
  - cloud/lambda/share/ws-default.presence.test.ts (new)
  - cloud/lambda/share/ws-disconnect.ts
  - cloud/lambda/share/ws-disconnect.test.ts
- **Learnings for future iterations:**
  - `addPeer` already accepts `PeerInfo`; extending the interface with a new field auto-propagates through DDB writes — but every existing test row literal must be updated or TS will flag missing `color`.
  - `routeFrame`'s `findConnRole` happens via Scan; ws-default `handleAuthPeer` now does a `getBySessionId` so it has the session row needed to know who to fan out to.
  - The ws-disconnect handler must build the presence envelope BEFORE the DDB write removes the peer entry (we capture `peer = found.peer` from the pre-write snapshot).
  - For `host-offline` broadcasts there's no real PeerInfo to draw from; use peerId/displayName='host' and a fixed gray color that still matches the `#[0-9a-fA-F]{6}` regex.
  - `ApiGatewayManagementApiClient.prototype.send` mocking from one test file leaks across files if not reset in `beforeEach` — each new test file must reinstall its own mock to be safe.
---

## 2026-05-31 - US-047
- `routeFrame` in cloud/lambda/share/ws-default.ts now special-cases `cursor` frames:
  oversize (>512 B body) → drop with `dropped:'oversize'`; payload re-validated via
  `CursorPayloadSchema.safeParse` → drop with `dropped:'invalid'`; `to` is forced to
  `'all'` so cursors are public to the session regardless of what the client sent.
- Added a module-scope token-bucket rate limiter (`Map<connId, {tokens, ts}>`,
  60 token bucket, 45 tokens/sec refill) that applies ONLY to `cursor` + `presence`
  types — RPC paths untouched. Drops log `dropped:'rate'` with no error reply.
- Exported `_resetRateLimiter()` for test isolation across files.
- New ws-default.cursor.test.ts covers fan-out, unauthed reject, 200-frame burst
  yielding exactly 60 broadcasts (with Date.now pinned), oversize drop, invalid
  payload drop, plus a bonus test that a peer-supplied `to:'host'` cursor is
  rewritten to `to:'all'` on the broadcast envelope.
- Full cloud suite: 96 tests / 0 fail / typecheck clean.
- Files changed:
  - cloud/lambda/share/ws-default.ts
  - cloud/lambda/share/ws-default.cursor.test.ts (new)
- **Learnings for future iterations:**
  - Module-scope mutable state (the rate-limit Map) leaks across tests *within the
    same file* and *across files in the same `bun test` invocation* — export a
    `_resetX()` helper and call it in `beforeEach` of every file that touches it.
  - For deterministic burst tests, pin `Date.now` to a constant value during the
    loop, then restore in `afterEach`. Token-bucket refill is time-driven so any
    real elapsed millis would leak ~0.045 tokens and change the count.
  - The envelope's `payload` is `z.unknown()`; per-type payload schemas
    (`CursorPayloadSchema` here) must be applied at the route handler — never
    assume envelope parsing has validated the inner payload.
  - When mutating an envelope before fan-out (e.g. forcing `to:'all'` for cursor),
    use a `routed = {...env, to:'all'}` copy and forward `routed`, never the
    original `env`, to both the broadcast loop and the audit log.
  - `(event.body ?? '').length` is the byte-cap check the PRD prescribes (counts
    JSON characters as bytes — fine for ASCII JSON, and matches the size logged
    in the existing audit line).
---

## 2026-05-31 - US-048
- Added `presenceLayer?: ReactNode` and `onCursorMove?: (coords, viewport) => void` to `SeeflowCanvasBaseProps` in `packages/canvas/src/components/seeflow-canvas.tsx`.
- `presenceLayer`, when provided, is rendered inside `<ViewportPortal>` between `<Background>` and `<GlowOverlay>`/`<Controls>` so it transforms with pan/zoom. Wrapper div carries `pointer-events:none` (per the AC) so the layer never eats pane clicks; individual cursors opt back in.
- `onPointerMove` handler now (when `onCursorMove` is wired) calls `rfInstance.screenToFlowPosition` + `getViewport`, forwarding `{ x, y, flowId: flowSlug ?? null }` and the viewport. Legacy path (no callback) does zero extra work — guarded on the function presence.
- Added `packages/canvas/src/components/seeflow-canvas.presence.test.tsx` (5 tests, all pass): asserts the slot lands inside `<ViewportPortal>` inside `<ReactFlow>`, that `ViewportPortal` is NOT mounted when the prop is absent, that the rfInstance ref is not touched when `onCursorMove` is absent, that the callback fires with finite x/y + viewport, and that `flowId` is `null` when `flowSlug` is omitted.
- Public types: `presenceLayer` and `onCursorMove` are part of `SeeflowCanvasBaseProps` which is unioned into the already-exported `SeeflowCanvasProps` — no new index.ts entries needed (the AC mention of "already re-exports SeeflowCanvasProps" is satisfied).
- Full canvas suite: 1266 tests / 0 fail / `bun run typecheck` clean across all workspaces / Biome format + lint clean.
- Files changed:
  - packages/canvas/src/components/seeflow-canvas.tsx (props, JSX, pointer handler)
  - packages/canvas/src/components/seeflow-canvas.presence.test.tsx (new)
- **Learnings for future iterations:**
  - The hook-shim test pattern in `packages/canvas/src/components/seeflow-canvas.test.tsx` is reusable for any new prop that needs DOM-less assertions on the rendered tree — copy `callSeeflowCanvas` + `renderWithHooks` and pin the ref slot by useRef declaration order. `RF_INSTANCE_REF_SLOT = 2` matches `flagsRef (0)`, `wrapperRef (1)`, `rfInstanceRef (2)` at the top of the SeeflowCanvas body — adding a new useRef before slot 2 would shift the constant.
  - `<ViewportPortal>` from `@xyflow/react` is the canvas-correct portal for content that must transform with pan/zoom; mounting cursor overlays in a plain absolutely-positioned wrapper would NOT track the viewport.
  - The `onPointerMove` guard pattern (`if (onCursorMove) { ... }` outside the existing `if (!drawingRef.current) return`) lets the same handler serve both presence emission AND the existing draw-mode rubber band — keep the new branch BEFORE the early-return so cursor emission works when the user isn't drawing.
  - Adding new SeeflowCanvas props: append fields to `SeeflowCanvasBaseProps` (NOT a per-mode union arm) — the discriminated `SeeflowCanvasProps` union already extends the base, so anything added there is reachable in every mode without duplicating the field across the union arms.
---

## 2026-05-31 - US-049
- Built `PeerCursorsLayer` overlay component in the canvas package, exporting `PeerCursor` + `PeerCursorsLayerProps` types from the Chrome section of `packages/canvas/src/index.ts`.
- Renders one `<div>` per peer (excluding `selfPeerId`) absolutely positioned at `translate({x}px, {y}px)` so the consumer can drop it inside a viewport-transformed wrapper (e.g. `<SeeflowCanvas presenceLayer={...} />`'s `<ViewportPortal>`). Each row: 14×14 SVG arrow in peer color with `drop-shadow(0 1px 1px rgba(0,0,0,0.35))` + name pill (peer color bg, white text, JetBrains Mono 11px, 4px radius, 4px/6px padding) anchored just past the arrow tip.
- Idle peers render at `opacity: 0.4`. All cursor wrappers carry `pointer-events: none` so the layer never eats pane clicks; the outer wrapper too.
- `transform 80ms linear` transition smooths the ~30 fps cursor stream from the relay without adding perceptible lag.
- 6 unit tests in `peer-cursors-layer.test.tsx` cover: selfPeerId exclusion, correct transforms for multiple peers + transition value, idle opacity + data-idle attr, displayName labels with pill bg/color, arrow size/fill/drop-shadow, empty `peers` array.
- Canvas suite: 1272 pass / 0 fail / typecheck + biome clean. (Pre-existing studio fs-watcher + server-timeout failures are unrelated.)
- Files changed:
  - packages/canvas/src/components/peer-cursors-layer.tsx (new)
  - packages/canvas/src/components/peer-cursors-layer.test.tsx (new)
  - packages/canvas/src/index.ts (Chrome-section export of `PeerCursorsLayer` + types)
- **Learnings for future iterations:**
  - When a component renders sub-elements via a helper component (e.g. `peers.map(p => <Cursor peer={p}/>)`) AND you want hook-shim-style tests that walk the JSX tree without a renderer, INLINE the sub-component's JSX into the parent — otherwise the returned tree contains `{type: Cursor, props: {peer}}` elements that have not been executed, so `findAll(el => el.type === 'svg')` finds nothing. Mirrors the `icon-renderer.test.tsx` pattern.
  - Bun's `bun test <filter>` matches by substring on file path AND requires the filename to contain `.test`/`.spec`/`_test_`/`_spec_`. A bare path filter like `bun test packages/canvas/src/components/seeflow-canvas` matches `seeflow-canvas.test.tsx` only (not `seeflow-canvas.presence.test.tsx`) due to how the substring is interpreted. Use the full file path or the closest unique substring.
  - The canvas package's hookless components (no `useState`/`useEffect`) can be unit-tested by calling them as plain functions — `PeerCursorsLayer(props)` returns the React element directly. No `renderWithHooks` shim needed (which is required only for components that call hooks). This matches the established pattern in `icon-renderer.test.tsx`.
  - I initially wrote files to `/Users/tuongaz/dev/seeflow/packages/...` instead of the worktree path `/Users/tuongaz/dev/seeflow/.claude/worktrees/realtime-share-design-doc/packages/...` — the absolute path resolves to the MAIN repo on branch `main`, not the active worktree. Always prefix file edits with the worktree's absolute path explicitly. Fixing required copying files into the worktree and reverting the main repo's working tree.
---
