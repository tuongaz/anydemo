# Ralph Progress Log
Started: Sun 31 May 2026 13:26:10 AEST
---

## Codebase Patterns
- All Live Share work happens in `/Users/tuongaz/dev/seeflow-viewer/cloud/` on branch `ralph/live-share-phase-1` (separate repo from the seeflow monorepo).
- Reference plan: `/Users/tuongaz/dev/seeflow/.claude/worktrees/realtime-share-design-doc/docs/plans/2026-05-31-live-share-phase-1-relay.md` (path within this worktree; PRD mentions a different path but the worktree copy is canonical here).
- Test runner: Bun (`bun test path/to/x.test.ts`). Typecheck: `bun run typecheck` (`tsc --noEmit`).
- Lambdas live under `cloud/lambda/<area>/`; CDK stack is `cloud/lib/seeflow-stack.ts`.
- AWS SDK deps pinned at 3.1048.0 (client-dynamodb, lib-dynamodb, client-apigatewaymanagementapi). Zod pinned at 3.23.8.
- CDK assertion tests under `cloud/lib/*.share.test.ts` MUST set `env: { account: '000000000000', region: 'us-east-1' }` on the stack — `route53.HostedZone.fromLookup` in `SeeflowStack` errors with `StackAccountRegionNotSpecified` otherwise. Pure region-only env (which the plan suggests) fails synth.
- `cloud/tsconfig.json` does NOT enable `noUnusedLocals`/`noUnusedParameters`, so declared-but-unused locals are fine for typecheck. Don't add `void x;` noise to silence them.

## 2026-05-31 13:35 - US-001
- Added `test`: `bun test` script and AWS SDK + Zod deps to `cloud/package.json`.
- Ran `bun install`; lockfile updated.
- Verified regression: `bun test lambda/uploader/index.test.ts` (5/5 pass) and `bun run typecheck` (clean).
- Files changed: `cloud/package.json`, `cloud/bun.lock`.
- **Learnings for future iterations:**
  - The PRD references `/Users/tuongaz/dev/seeflow/docs/plans/...` but in this worktree the plan file lives at `/Users/tuongaz/dev/seeflow/.claude/worktrees/realtime-share-design-doc/docs/plans/2026-05-31-live-share-phase-1-relay.md`. Read from there.
  - Branch `ralph/live-share-phase-1` was created from main in the seeflow-viewer repo; all subsequent commits go there.
  - `bun install` automatically alphabetizes dependency order in package.json; do not be surprised when bun.lock changes order.
  - Existing uploader test at `cloud/lambda/uploader/index.test.ts` is the canonical baseline regression check.
---

## 2026-05-31 13:55 - US-002
- Added Live Share DDB table + GSI + WebSocket API skeleton with `$connect`/`$disconnect`/`$default` placeholder routes inside `SeeflowStack`.
- New imports: `aws-cdk-lib/aws-dynamodb`, `aws-cdk-lib/aws-apigatewayv2-integrations` (namespace) on top of existing `HttpLambdaIntegration` named import.
- DDB table `seeflow-share-sessions`: `sessionId` HASH, PAY_PER_REQUEST, `ttl` TTL attr, `RemovalPolicy.DESTROY`, GSI `token-index` on `token` (PROJECTION_ALL).
- WebSocket API uses `$request.body.action` route selection expression; stage name `prod` with autoDeploy; `CfnOutput` `ShareWebSocketUrl` emits the stage URL.
- All three WS routes wired through a single placeholder lambda `cloud/lambda/share/_placeholder.ts` (returns 200) — to be replaced in US-009..US-011.
- Created `cloud/lib/seeflow-stack.share.test.ts` with the three CDK assertion tests (table+TTL, GSI, WS API+routes). All 3 pass.
- Regression: `bun test lambda/uploader/index.test.ts` still 5/5; `bun run typecheck` clean.
- Files changed: `cloud/lib/seeflow-stack.ts`, `cloud/lib/seeflow-stack.share.test.ts`, `cloud/lambda/share/_placeholder.ts`.
- **Learnings for future iterations:**
  - `route53.HostedZone.fromLookup` in `SeeflowStack` requires both account AND region on the stack env; the plan's `env: { region: 'us-east-1' }` is insufficient. Tests MUST pass `env: { account: '000000000000', region: 'us-east-1' }`.
  - The existing CDK stack already had a `HttpLambdaIntegration` named import from `aws-cdk-lib/aws-apigatewayv2-integrations`; for the WebSocket integration the plan uses a namespace import (`apigwv2Integrations.WebSocketLambdaIntegration`). Both imports from the same module are fine to keep — don't try to consolidate or rename.
  - `apigwv2.WebSocketApi`/`WebSocketStage` are stable in `aws-cdk-lib` 2.254 (no `alpha` package needed).
  - The CDK assertion test `resourceCountIs('AWS::ApiGatewayV2::Api', 2)` is the right count once the share WS API is added (existing HTTP API + new WS API).
---

## 2026-05-31 13:35 - US-003
- Implemented share id generators per plan Task 2.
- Created `cloud/lambda/share/shared/ids.ts` (urlSafe + newSessionId 8ch + newToken 43ch + newPeerId).
- Created `cloud/lambda/share/shared/ids.test.ts` (4 tests, all pass).
- `bun test lambda/share/shared/ids.test.ts`: 4/4 pass. `bun run typecheck`: clean.
- Files changed: `cloud/lambda/share/shared/ids.ts`, `cloud/lambda/share/shared/ids.test.ts`.
- **Learnings for future iterations:**
  - The `shared/` subdir under `lambda/share/` is the canonical home for cross-handler helpers (ids, jwt, envelope, ddb, sessions). Co-located tests use `bun:test`.
  - `randomBytes(6)` → base64url → exactly 8 chars; `randomBytes(32)` → base64url → exactly 43 chars (no `=` padding). These length invariants are asserted by the tests; do not change byte counts.
---

## 2026-05-31 14:05 - US-004
- Implemented HMAC-SHA256 peer token (compact custom signed token — not a full JWT) per plan Task 3.
- Created `cloud/lambda/share/shared/jwt.ts` exporting `PeerPayload`, `mintPeerToken`, `verifyPeerToken`, `VerifyResult` (discriminated union with reason: malformed | bad-signature | expired). Uses `node:crypto` only — no external dep.
- Created `cloud/lambda/share/shared/jwt.test.ts` (4 tests: round-trip, tampered body, expired, wrong-secret). All pass.
- `bun test lambda/share/shared/jwt.test.ts`: 4/4 pass. `bun run typecheck`: clean. Uploader regression: 5/5.
- Files changed: `cloud/lambda/share/shared/jwt.ts`, `cloud/lambda/share/shared/jwt.test.ts`.
- **Learnings for future iterations:**
  - The token format is `head.body.sig`, all three parts url-safe-base64. Reuses `urlSafe(buf)` from `shared/ids.ts` — keep that helper as the single base64url encoder for the share module.
  - `verifyPeerToken` decodes the body with `Buffer.from(body, 'base64')` (Node's base64 decoder tolerates `-`/`_` in practice, so the url-safe body round-trips). Don't switch to `'base64url'` unless future Node tightens this.
  - Signature comparison uses `timingSafeEqual` with explicit length check — required because `timingSafeEqual` throws on length mismatch.
  - Tests use `'a'.repeat(32)` as the secret (32-byte string is ample for HS256). TTL of `-1` cleanly produces an already-expired token.
---

## 2026-05-31 - US-005
- Implemented envelope Zod schema + parser per plan Task 4.
- Created `cloud/lambda/share/shared/envelope.ts` exporting `EnvelopeSchema` (v=literal(1), 14-type enum, optional id, required from, optional to as string|'host'|'all', unknown payload), `Envelope` type, and `parseEnvelope` returning a discriminated union `{ok:true, envelope}` | `{ok:false, reason}`.
- Created `cloud/lambda/share/shared/envelope.test.ts` (5 tests: rpc happy path, wrong version, unknown type, auth-host, auth-peer). All pass.
- `bun test lambda/share/shared/envelope.test.ts`: 5/5. `bun run typecheck`: clean. Uploader regression: 5/5.
- Files changed: `cloud/lambda/share/shared/envelope.ts`, `cloud/lambda/share/shared/envelope.test.ts`.
- **Learnings for future iterations:**
  - `safeParse` from zod yields `{success, data}` / `{success, error}`; the envelope module reshapes that into the project's `{ok, envelope/reason}` shape. Future handlers should import `parseEnvelope` (not `EnvelopeSchema` directly) so error reporting stays consistent.
  - The 14-value `type` enum is the authoritative list of envelope frame types for Phase 1 — extend here when adding new frame types in later phases.
  - `to` accepts arbitrary string OR the literals `'host'` / `'all'`. Routing in ws-default (US-011) keys off these three cases.
---

## 2026-05-31 - US-006
- Implemented DDB session helpers per plan Task 5.
- Created `cloud/lambda/share/shared/ddb.ts` exporting `doc` (DocumentClient with removeUndefinedValues) and `TABLE` (env SHARE_TABLE, defaults to `seeflow-share-sessions`).
- Created `cloud/lambda/share/shared/sessions.ts` exporting `PeerInfo`, `SessionRow`, `createSession`, `getBySessionId`, `getByToken`, `setHostConnId`, `addPeer`, `removePeer`, `touch`. All mutations refresh `lastActivity` + `ttl` (`now + 30min`).
- Created `cloud/lambda/share/shared/sessions.test.ts` with in-memory fake of `doc.send` covering Put/Get/Query/Update; 4 tests pass.
- `bun test lambda/share/shared/sessions.test.ts`: 4/4. `bun run typecheck`: clean. Uploader regression: 5/5.
- Files changed: `cloud/lambda/share/shared/ddb.ts`, `cloud/lambda/share/shared/sessions.ts`, `cloud/lambda/share/shared/sessions.test.ts`.
- **Learnings for future iterations:**
  - The in-memory fake injects `(ddbMod.doc as any).send = fakeSend` in `beforeEach`. This relies on `ddb.ts` exporting `doc` as a re-assignable property — works because `DynamoDBDocumentClient` is a class instance whose `send` is mutable. Future handler tests should mock the same way (replace `send` on the singleton) rather than mocking `@aws-sdk/lib-dynamodb` imports.
  - `IDLE_TTL_SECONDS = 30 * 60`. All write operations (`createSession`, `setHostConnId`, `addPeer`, `removePeer`, `touch`) bump both `lastActivity` (now in seconds) AND `ttl` (now+30min) — DDB TTL attribute is `ttl`, configured in CDK as the table TTL attribute.
  - `PeerInfo.joinedAt` is server-set inside `addPeer` (callers omit it); type marks it optional. `role` is locked to the literal `'editor'` for Phase 1.
  - `getByToken` uses the `token-index` GSI (created in US-002) with `KeyConditionExpression` + ExpressionAttribute names because `token` is a reserved-ish keyword in some DDB contexts; the `#tk` alias avoids any ambiguity.
---

## 2026-05-31 - US-007
- Implemented POST /api/share/sessions handler per plan Task 6.
- Created `cloud/lambda/share/session-create.ts` exporting `handler` that reads `SHARE_WS_URL` from env, generates `sessionId` (8 ch) + `token` (43 ch) + `hostKey` (43 ch) via `shared/ids`, calls `createSession`, returns JSON with CORS headers.
- Created `cloud/lambda/share/session-create.test.ts` (2 tests: happy path verifying all 4 response fields + CORS headers + DDB row written, and 500 when SHARE_WS_URL missing). All pass.
- `bun test lambda/share/session-create.test.ts`: 2/2. `bun run typecheck`: clean. Uploader regression: 5/5.
- Files changed: `cloud/lambda/share/session-create.ts`, `cloud/lambda/share/session-create.test.ts`.
- **Learnings for future iterations:**
  - For HTTP-handler tests, mock `doc.send` (same pattern as `sessions.test.ts`) rather than trying to reassign named exports like the plan suggests (`(sessions as any).createSession = mock(...)`). ESM bindings are immutable in Bun; the doc.send approach is proven to work and tests the full handler→DDB path end-to-end.
  - The CORS object lives inline in each handler module (Task 6 + 7 both repeat it). For now don't extract to shared/cors.ts — only 2 handlers use CORS and the plan keeps them isolated. Reconsider if more HTTP handlers land.
  - Lambda return type `APIGatewayProxyResultV2` accepts the `{statusCode, headers, body}` shape directly — no `isBase64Encoded` needed.
---

## 2026-05-31 - US-008
- Implemented POST /api/share/join handler per plan Task 7.
- Created `cloud/lambda/share/session-join.ts` exporting `handler` that reads SHARE_WS_URL + SHARE_JWT_SECRET from env, validates body (token + displayName), trims displayName + caps at 40 chars, calls `getByToken`, mints 5-min peer JWT via `mintPeerToken`, returns JSON with CORS headers and `{sessionId, peerId, displayName, wsUrl, peerJwt, hostOnline, flowList:[]}`.
- Created `cloud/lambda/share/session-join.test.ts` (5 tests: happy path with all response fields, 404 unknown token, 400 missing displayName, displayName trim+cap, 500 missing SHARE_JWT_SECRET). All pass.
- `bun test lambda/share/session-join.test.ts`: 5/5. `bun run typecheck`: clean. Uploader regression: 5/5.
- Files changed: `cloud/lambda/share/session-join.ts`, `cloud/lambda/share/session-join.test.ts`.
- **Learnings for future iterations:**
  - Test mocks `doc.send` via the `ddbMod.doc as any` pattern (same as session-create.test.ts) and dispatches on `cmd.constructor.name === 'QueryCommand'`, reading `cmd.input.ExpressionAttributeValues[':t']` for the token. This matches how `getByToken` issues its QueryCommand against the `token-index` GSI — do NOT try the plan's `(sessions as any).getByToken = mock(...)` approach (ESM bindings are immutable in Bun).
  - `hostOnline: Boolean(session.hostConnId)` — null vs string. The fake row uses `'host-conn'` so happy path asserts true; future tests for offline host should set `hostConnId: null`.
  - displayName trim + 40-char cap is done in the handler before the JWT mint; the cap was added as an explicit test to guard the acceptance criterion.
---

## 2026-05-31 - US-009
- Implemented WebSocket $connect handler per plan Task 8a.
- Created `cloud/lambda/share/ws-connect.ts` exporting `handler` that returns `{statusCode: 200, body: 'ok'}` — auth happens in the first frame on $default (US-011) so secrets never appear in connect URL/headers.
- Created `cloud/lambda/share/ws-connect.test.ts` (1 test: returns 200). Passes.
- `bun test lambda/share/ws-connect.test.ts`: 1/1. `bun run typecheck`: clean. Uploader regression: 5/5.
- Files changed: `cloud/lambda/share/ws-connect.ts`, `cloud/lambda/share/ws-connect.test.ts`.
- **Learnings for future iterations:**
  - $connect intentionally has no DDB calls — keeps it cheap and avoids gating connections on a token in the URL (which would be logged). The disconnect lambda (US-010) and default lambda (US-011) carry all the real work.
  - The handler signature uses `APIGatewayProxyWebsocketEventV2` + `APIGatewayProxyResultV2` from `aws-lambda`, same shape the disconnect/default lambdas will use.
---

## 2026-05-31 - US-010
- Implemented WebSocket $disconnect handler per plan Task 8b.
- Created `cloud/lambda/share/ws-disconnect.ts`: handler `Scan`s `ShareSessions`, finds the row containing `connId` (as `hostConnId` or key in `peers`), calls `setHostConnId(sessionId, null)` for host or `removePeer(sessionId, connId)` for peer, returns 200. Unknown connection ⇒ 200 no-op.
- `findByConnId` lives inside the same file (small helper) per plan; not extracted to `shared/` because the equivalent helper in ws-default returns extra `peerId` data and we don't want to over-share.
- Created `cloud/lambda/share/ws-disconnect.test.ts` (3 tests: host-disconnect clears `hostConnId`, peer-disconnect removes peer entry, unknown-conn is no-op). All pass.
- Same `doc.send` swap pattern as `sessions.test.ts` / `session-create.test.ts` — fake handles ScanCommand and UpdateCommand (host clear + peer remove).
- `bun test lambda/share/ws-disconnect.test.ts`: 3/3. `bun run typecheck`: clean. Uploader regression: 5/5.
- Files changed: `cloud/lambda/share/ws-disconnect.ts`, `cloud/lambda/share/ws-disconnect.test.ts`.
- **Learnings for future iterations:**
  - The disconnect lambda scans the whole table — plan calls this out as acceptable because session count stays small (TTL 30 min). Don't try to optimize with a connId GSI yet; ws-default already does the same scan-by-connId pattern (US-011 will reuse it as `findConnRole` returning the extra `peerId`).
  - Tests stub the `ScanCommand` branch by returning `Array.from(store.values())` — handy boilerplate to copy when writing the ws-default test in US-011 (which also needs PostToConnection mocked).
  - For typed test row builders, build via a `row(over: Partial<SessionRow>): SessionRow` factory that spreads defaults — easier than crafting full rows inline and keeps tests focused on the field that matters per case.
---

## 2026-05-31 - US-011
- Implemented WebSocket `$default` handler per plan Task 8c.
- Created `cloud/lambda/share/ws-default.ts`: `handler` JSON-parses `event.body`, runs `parseEnvelope`, dispatches `auth-host` / `auth-peer` / `routeFrame`.
  - `handleAuthHost`: 401 on wrong hostKey or missing session; calls `setHostConnId` on success.
  - `handleAuthPeer`: reads `SHARE_JWT_SECRET`, 401 on invalid/expired JWT, calls `addPeer` on success (displayName trimmed at 40 chars).
  - `routeFrame`: 401 if `findConnRole` returns undefined; touches the session; builds `targets[]` excluding sender; `to:'all'|undefined` fans out to host+peers, `to:'host'` to hostConnId, otherwise direct PostToConnection to the given connId; `GoneException` triggers `setHostConnId(null)` (host) or `removePeer` (peer).
  - Only logs `{type, from, to, sizeBytes}` — never the payload.
- Created `cloud/lambda/share/ws-default.test.ts` (9 tests): auth-host wrong/right key, auth-peer expired/valid, route-from-unauthed, route-to-host (single Post), route-to-all (fan-out to host + other peers, sender excluded), GoneException → host cleanup, GoneException → peer cleanup.
- Mocking pattern: swap `(ddbMod.doc as any).send = fakeDocSend` (same as prior tests) AND swap `(ApiGatewayManagementApiClient.prototype as any).send = fakeApiSend`. `GoneException` from `@aws-sdk/client-apigatewaymanagementapi` constructor takes `{$metadata, message}`.
- `_placeholder.ts` was NOT deleted (acceptance criterion deferred to US-012). Deleting it now breaks the existing CDK assertion tests from US-002 because `seeflow-stack.ts` still references its file path in the three placeholder route integrations. US-012 replaces those integrations with the real lambdas; that is the right moment to remove the placeholder file.
- `bun test lambda/share/ws-default.test.ts`: 9/9 pass.
- `bun test lib/seeflow-stack.share.test.ts lambda/share/ws-default.test.ts lambda/uploader/index.test.ts`: 17/17 pass.
- `bun run typecheck`: clean.
- Files changed: `cloud/lambda/share/ws-default.ts`, `cloud/lambda/share/ws-default.test.ts`.
- **Learnings for future iterations:**
  - To mock `ApiGatewayManagementApiClient` in Bun tests, swap `ApiGatewayManagementApiClient.prototype.send = mockFn` in `beforeEach`. The handler constructs a fresh client per invocation, so prototype mutation catches all of them. To simulate `GoneException`, throw `new GoneException({$metadata: {}, message: 'gone'})` (constructor signature from `@aws-sdk/client-apigatewaymanagementapi`).
  - The `findConnRole` Scan returns full `SessionRow` items — reuse the existing in-memory `store` Map and have the fake's `ScanCommand` branch return `{Items: Array.from(store.values())}` (mirrors the `ws-disconnect.test.ts` setup).
  - Acceptance criteria "Delete _placeholder.ts" was deferred to US-012: deleting it now breaks the CDK NodejsFunction synth because the stack still references the placeholder file path. The principle "ALL commits must pass quality checks" takes precedence over a literal file deletion that creates a temporary broken state. US-012 will remove the placeholder as part of swapping integrations.
  - The routing branch for an explicit connId target now guards `env.to !== connId` so the sender can't address themselves; the plan didn't spell this out but it preserves the "sender never receives its own frame" invariant the acceptance criteria called out.
  - Envelope `payload` is typed `z.unknown()` — handlers cast via `env.payload as {...}` and then null-check fields. Don't try to extend the EnvelopeSchema with per-frame payload shapes for v1; keep payload typing inside each handler.
---
