## Codebase Patterns
- CSS @import must come before @tailwind directives in index.css
- Shadcn HSL token format: `--token: H S% L%` (no `hsl()` wrapper in the var definition)
- Canvas-specific rules in index.css live outside @layer blocks to maintain specificity over xyflow library defaults
- `bun run format` auto-converts single quotes to double quotes (Biome config)
- design.html uses the same shadcn HSL tokens as index.css; design aliases (--bg, --surface, etc.) point to `hsl(var(--shadcn-name))`
- `--border` clashes with shadcn's raw `--border` token name — all `var(--border)` usages must be wrapped as `hsl(var(--border))` in design.html

# Ralph Progress Log
Started: Mon 18 May 2026 11:48:07 AEST
---

## 2026-05-18 - US-001
- Replaced light :root + .dark CSS blocks with single permanent dark :root using zinc-950/emerald palette
- Added Google Fonts @import for Inter + JetBrains Mono at top of file
- Added dotted grid background-image to body (radial-gradient 32px)
- Added extended custom vars: --emerald-glow, --shadow-card, --shadow-window, --shadow-glow-ok, --shadow-glow-pending, --ring-selected, --bg-canvas, --amber, --amber-hi, --danger, --font-mono
- All canvas-specific rules (handles, edges, z-index, cursors) preserved unchanged
- Files changed: apps/web/src/index.css
- **Learnings for future iterations:**
  - The @import must be the very first line before @tailwind directives
  - Biome formats single quotes to double quotes — just run `bun run format` to fix
  - Canvas rules are intentionally outside @layer blocks for cascade specificity
  - The .dark block was removed entirely; the app is now permanently dark-themed
---

## 2026-05-18 - US-002
- Added shadcn HSL tokens to design.html :root (--background, --foreground, --card, --primary, --primary-foreground, --border, --input, --muted-foreground) matching index.css exactly
- Changed design alias vars (--bg, --surface, --border-hi, --text, --text-mute, --emerald, --emerald-hi) from hex to `hsl(var(--shadcn-name))`
- `--border` name collides with shadcn raw token; resolved by defining `--border: 240 3.7% 15.9%` and updating all 23 usages to `hsl(var(--border))`
- Custom vars with no shadcn equivalent (--bg-canvas, --bg-deep, --text-dim, --text-faint, --emerald-glow, --amber, --amber-hi, --danger, shadows) kept as-is
- Google Fonts import (Inter + JetBrains Mono) was already present — no change needed
- Files changed: design/design.html
- **Learnings for future iterations:**
  - `--border` is a shadcn token name AND a design alias — defining it as raw HSL means all CSS/inline usages must use `hsl(var(--border))` not bare `var(--border)`
  - Edit tool's replace_all is safe for `var(--border)` → `hsl(var(--border))` since the :root definition doesn't contain `var(--border)` as a substring
  - design.html is a standalone file; it must define all shadcn tokens locally (it cannot import index.css)
---

## 2026-05-18 - US-003
- Updated button.tsx primary variant: changed `text-primary-foreground` to `text-zinc-950` for dark text on emerald background
- Ghost variant: changed `hover:bg-accent hover:text-accent-foreground` to `hover:bg-muted hover:text-foreground` (more semantically correct zinc surface)
- Outline variant: changed `hover:bg-accent hover:text-accent-foreground` to `hover:bg-muted hover:text-foreground` (removes any white-background risk)
- Added `disabled:text-muted-foreground` to base classes for muted zinc disabled state
- Files changed: apps/web/src/components/ui/button.tsx
- **Learnings for future iterations:**
  - `--primary-foreground` is light emerald (#34d399) — NOT suitable for text on emerald buttons; use `text-zinc-950` or `text-background` for dark text on emerald backgrounds
  - `--accent` and `--muted` resolve to the same zinc-800 value with our tokens; prefer `muted` semantically for hover surfaces
  - All checks passed without server running — button.tsx is pure Tailwind/CVA with no runtime deps
  - Browser verification skipped (no browser MCP tools available in this session)
---

## 2026-05-18 - US-005
- Removed hardcoded `color: '#0f172a'` from button inline style in header.tsx so text inherits `--foreground` (zinc-50)
- Added `border-border` to header element className to explicitly use the design token for the bottom border color
- App.tsx loading state already used `bg-background text-muted-foreground` — no changes needed
- App.tsx root div already used `bg-background text-foreground` — no changes needed
- Files changed: apps/web/src/components/header.tsx
- **Learnings for future iterations:**
  - `#0f172a` is slate-900 — invisible on the dark zinc-950 background; always remove hardcoded dark/light hex colors from components
  - `border-b` alone works via shadcn's global `* { border-color: hsl(var(--border)) }` base rule, but adding `border-border` is more explicit and matches acceptance criteria
  - App.tsx already had correct token classes; only header.tsx needed changes
  - Browser verification skipped (no browser MCP tools available in this session)
---

## 2026-05-18 - US-006
- Replaced `rounded-md` with `rounded-lg` on demo picker button cards
- Added explicit `border-border` to button card (was just `border` before)
- Removed `hover:bg-accent hover:text-accent-foreground` (would flip to white surface)
- Added `hover:border-input` for border-brightening hover (zinc-700 vs zinc-800 default)
- Added `style={{ boxShadow: 'var(--shadow-card)' }}` inline style for card shadow
- Files changed: apps/web/src/pages/studio-home.tsx
- **Learnings for future iterations:**
  - `--shadow-card` must be applied via inline style since Tailwind has no utility for it; `style={{ boxShadow: 'var(--shadow-card)' }}` is the pattern
  - For border-brightening hover, `hover:border-input` (zinc-700) vs default `border-border` (zinc-800) is a subtle but effective design signal
  - Browser verification skipped (no browser MCP tools available in this session)
---

## 2026-05-18 - US-004
- sheet.tsx: changed `bg-background` to `bg-card border-border` in sheetVariants base class
- dialog.tsx: changed `bg-background` to `bg-card` and `border` to `border border-border` in DialogContent
- command.tsx: changed `bg-popover text-popover-foreground` to `bg-card text-foreground` in Command base; changed `aria-selected:bg-accent aria-selected:text-accent-foreground` to `aria-selected:bg-muted aria-selected:text-foreground` in CommandItem
- Files changed: apps/web/src/components/ui/sheet.tsx, apps/web/src/components/ui/dialog.tsx, apps/web/src/components/ui/command.tsx
- **Learnings for future iterations:**
  - `--popover` token is not explicitly defined in our token set; `bg-card` is the correct zinc surface replacement for `bg-popover`
  - `bg-accent` and `bg-muted` resolve to the same zinc-800 with our tokens; use `bg-muted` semantically for hover/selected surfaces
  - Sheet side variants (border-b, border-t, border-r, border-l) set which border sides render; adding `border-border` to the base sets the color for all sides
  - Browser verification skipped (no browser MCP tools available in this session)
---

## 2026-05-18 - US-007
- Added explicit `text-foreground` to `<h1>` so heading uses the design token rather than implicit inheritance
- Added `border-border` to the command bar div alongside existing `border` for explicit token usage
- Component was already mostly correct (bg-background, bg-muted, text-muted-foreground all in place); no hardcoded colors to remove
- Files changed: apps/web/src/components/empty-state.tsx
- **Learnings for future iterations:**
  - `border` alone works via shadcn global reset but adding `border-border` is preferred for explicitness
  - Heading elements without an explicit color class inherit from the parent chain — but `text-foreground` should be added for clarity when a design token should apply
  - Browser verification skipped (no browser MCP tools available in this session)
---

## 2026-05-18 - US-008
- Toolbar container: changed `bg-background/95` to `bg-card` for correct zinc-900 surface
- Active tool buttons: changed `bg-primary text-primary-foreground hover:bg-primary/90` to emerald pill pattern `bg-primary/10 text-primary border border-primary/30`
- Inactive tool buttons: changed `hover:bg-accent hover:text-accent-foreground` to `hover:bg-muted` for zinc surface treatment
- Applied same changes to: shape picker trigger button, illustrative-shapes popover items, and icon picker anchor button
- Files changed: apps/web/src/components/canvas-toolbar.tsx
- **Learnings for future iterations:**
  - The emerald pill pattern for active state is: `bg-primary/10 text-primary border border-primary/30` (translucent background, colored text, subtle border)
  - `bg-background/95` (near-transparent overlay on background) should be replaced with `bg-card` for solid opaque zinc-900 surfaces
  - All four button variants in canvas-toolbar (primary shapes, shape picker trigger, shape picker menu items, icon picker trigger) need consistent active/inactive class treatment
  - Browser verification skipped (no browser MCP tools available in this session)
---
