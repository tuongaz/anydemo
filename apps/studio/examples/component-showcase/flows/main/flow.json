{
  "version": 2,
  "name": "Component Showcase",
  "description": "Three component nodes demonstrating json-render-powered reactive UIs on the canvas: client-side state with set actions, a form with live $state binding plus a Table, and a multi-series Chart with Tabs.",
  "nodes": [
    {
      "id": "counter",
      "type": "component",
      "data": {
        "name": "Counter",
        "description": "Card + Metric + Buttons driven by set-kind actions."
      }
    },
    {
      "id": "form",
      "type": "component",
      "data": {
        "name": "Form Demo",
        "description": "Card + Input + Select + Table — live $state binding via set with $param."
      }
    },
    {
      "id": "chart",
      "type": "component",
      "data": {
        "name": "Chart & Tabs",
        "description": "Card + Tabs + Metric + multi-series bar Chart — value/onChange binding plus a recharts-lazy Chart."
      }
    }
  ],
  "connectors": []
}
